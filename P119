#include <stdio.h>
int main(){
    int n; scanf("%d",&n); int arr[n]; for(int i=0;i<n;i++) scanf("%d",&arr[i]);
    int f[n+1]; for(int i=0;i<=n;i++) f[i]=0;
    for(int i=0;i<n;i++){
        if(f[arr[i]]){ printf("%d\n",arr[i]); break; }
        f[arr[i]]=1;
    }
    return 0;
}
