#include <stdio.h>
int main(){
    int n; scanf("%d",&n);
    int total = n*(n+1)/2, sum=0, pivot=-1;
    for(int i=1;i<=n;i++){
        sum+=i;
        if(sum == total-sum+i){ pivot=i; break; }
    }
    printf("%d\n",pivot);
    return 0;
}
