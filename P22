#include <stdio.h>

int main() {
    float cp, sp, percent;

    printf("Enter Cost Price: ");
    scanf("%f", &cp);

    printf("Enter Selling Price: ");
    scanf("%f", &sp);

    if (sp > cp) {
        percent = ((sp - cp) / cp) * 100;
        printf("\nProfit Percentage = %.2f%%\n", percent);
    } else if (cp > sp) {
        pe
