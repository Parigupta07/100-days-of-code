#include <stdio.h>

int main() {
    int num, bin[32], i = 0;

    printf("Enter a number: ");
    scanf("%d", &num);

    if (num == 0) {
        printf("Binary = 0");
        return 0;
    }

    while (num > 0) {
        bin[i] = num % 2;
        num /= 2;
        i++;
    }

    printf("Binary = ");
    for (i =
